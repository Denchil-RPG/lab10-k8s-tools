apiVersion: v1
kind: Service
metadata:
  name: service-devops
  labels:
    svc: flask-front
spec:
  type: LoadBalancer
  externalIPs:
  - 10.0.2.15
  ports:
  - port: 12345
    targetPort: 5000
    name: flask-port
  selector:
    app: flask-app
